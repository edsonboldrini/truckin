import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:truckin/app/modules/auth/sign_in/sign_in_controller.dart';
import 'package:truckin/app/modules/auth/auth_module.dart';

void main() {
  initModule(AuthModule());
  // SignInController signin;
  //
  setUp(() {
    //     signin = AuthModule.to.get<SignInController>();
  });

  group('SignInController Test', () {
    //   test("First Test", () {
    //     expect(signin, isInstanceOf<SignInController>());
    //   });

    //   test("Set Value", () {
    //     expect(signin.value, equals(0));
    //     signin.increment();
    //     expect(signin.value, equals(1));
    //   });
  });
}
