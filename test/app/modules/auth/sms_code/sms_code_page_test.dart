import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:truckin/app/modules/auth/sms_code/sms_code_page.dart';

void main() {
  testWidgets('SmsCodePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SmsCodePage(title: 'SmsCode')));
    //  final titleFinder = find.text('SmsCode');
    //  expect(titleFinder, findsOneWidget);
  });
}
