import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:truckin/app/modules/auth/sms_code/sms_code_controller.dart';
import 'package:truckin/app/modules/auth/auth_module.dart';

void main() {
  initModule(AuthModule());
  // SmsCodeController smscode;
  //
  setUp(() {
    //     smscode = AuthModule.to.get<SmsCodeController>();
  });

  group('SmsCodeController Test', () {
    //   test("First Test", () {
    //     expect(smscode, isInstanceOf<SmsCodeController>());
    //   });

    //   test("Set Value", () {
    //     expect(smscode.value, equals(0));
    //     smscode.increment();
    //     expect(smscode.value, equals(1));
    //   });
  });
}
